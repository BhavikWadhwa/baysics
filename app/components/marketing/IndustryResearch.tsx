"use client";

import { useState } from "react";
import { Bar, BarChart, CartesianGrid, Cell, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { X } from "lucide-react";
import { industrySources, trainingPerception, turnoverByRole } from "../../data/industry-stats";
import { SourceCitation } from "../shared/OperationsUI";

export function IndustryResearch(){const [sourcesOpen,setSourcesOpen]=useState(false);return <section className="research-section section" id="research"><div className="container"><p className="eyebrow" style={{color:"var(--tortilla)"}}>The operating cost of churn</p><h2 className="display section-title">Restaurant turnover isn&apos;t just an HR problem. It&apos;s an operating cost.</h2><div className="research-grid"><article className="research-card"><span className="research-kicker">~75%</span><h3 className="display">Average restaurant employee turnover</h3><p>Quick-service turnover can exceed 130%.</p><div className="research-chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={turnoverByRole} layout="vertical" margin={{left:10,right:25}}><CartesianGrid strokeDasharray="3 3" stroke="#506055"/><XAxis type="number" domain={[0,50]} tickFormatter={x=>`${x}%`} stroke="#fff"/><YAxis dataKey="role" type="category" width={105} stroke="#fff"/><Tooltip formatter={value=>`${value}%`}/><Bar dataKey="value" fill="#F2CB4D" radius={[0,8,8,0]}/></BarChart></ResponsiveContainer></div><SourceCitation {...industrySources.homebase}/></article><article className="research-card light"><span className="research-kicker chilli">74%</span><h3 className="display">receive two weeks or less of training</h3><p>Longer training periods were associated with more positive employee ratings.</p><div className="research-chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={trainingPerception}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="duration"/><YAxis domain={[0,100]} tickFormatter={x=>`${x}%`}/><Tooltip formatter={(_,__,entry)=>entry.payload.label}/><Bar dataKey="value" radius={[8,8,0,0]}>{trainingPerception.map((_,i)=><Cell key={i} fill={["#D85032","#E9A23B","#72865B"][i]}/>)}</Bar></BarChart></ResponsiveContainer></div><div className="bar-labels">{trainingPerception.map(x=><strong key={x.duration}>{x.label}</strong>)}</div><SourceCitation {...industrySources.toast}/></article></div><article className="cost-story"><div><span className="research-kicker">$2,305+</span><p>Estimated hard cost associated with replacing one hourly restaurant employee.</p></div><div><span className="research-kicker">$5,864</span><p>Broader turnover-cost model reported from cited industry research.</p></div><div><span className="research-kicker">$821</span><p>Of the broader estimate attributed to orientation and training.</p></div><p className="cost-note">These figures use different methodologies and should not be added together.</p><SourceCitation {...industrySources.homebase}/></article><aside className="honesty-note"><strong>What software cannot solve</strong><p>Training software cannot solve compensation, management quality, workplace culture, or scheduling by itself. baysics focuses on reducing onboarding friction, improving knowledge accessibility, standardizing training, and reducing operational inefficiencies associated with repeatedly teaching foundational information.</p><button className="btn btn-light" onClick={()=>setSourcesOpen(true)}>Sources &amp; Methodology</button></aside></div>{sourcesOpen&&<div className="modal-backdrop"><div className="modal sources-modal" role="dialog" aria-modal="true" aria-labelledby="sources-title"><button className="round-btn close" onClick={()=>setSourcesOpen(false)} aria-label="Close sources"><X/></button><h2 id="sources-title" className="display">Sources &amp; Methodology</h2><p>External statistics are kept separate from fictional product-demo operations data.</p><SourceCitation {...industrySources.homebase}/><SourceCitation {...industrySources.toast}/><button className="btn btn-dark" onClick={()=>setSourcesOpen(false)}>Done</button></div></div>}</section>}

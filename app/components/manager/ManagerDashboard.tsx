"use client";

import Link from "next/link";
import { AlertCircle, ArrowRight, BookOpenCheck, CircleCheck, Clock3, UserCheck, Users } from "lucide-react";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { completionChart, employees, managerActivity } from "../../data/mock-data";

export function ManagerDashboard(){return <><div><p className="eyebrow" style={{color:"var(--chilli)"}}>Overview · {"Hoy"}</p><h1 className="display app-title">Team readiness at a glance.</h1><p className="muted">A practical view of learning progress before the next shift.</p></div><div className="stat-grid manager-stats"><ManagerStat icon={<Users/>} value="18" label="Active employees"/><ManagerStat icon={<BookOpenCheck/>} value="6" label="Currently training"/><ManagerStat icon={<CircleCheck/>} value="78%" label="Avg. completion"/><ManagerStat icon={<AlertCircle/>} value="4" label="Overdue modules"/><ManagerStat icon={<UserCheck/>} value="3" label="Ready for sign-off"/></div><div className="manager-grid"><section className="panel"><div className="row between"><div><p className="eyebrow muted">Completion overview</p><h2 className="panel-title">Pathway progress</h2></div><span className="tag">Last 5 weeks</span></div><div className="chart-wrap"><ResponsiveContainer width="100%" height="100%"><AreaChart data={completionChart} margin={{top:15,right:15,left:-20,bottom:0}}><defs><linearGradient id="completion" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#D85032" stopOpacity={.32}/><stop offset="95%" stopColor="#D85032" stopOpacity={0}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3" stroke="#e1d5c4"/><XAxis dataKey="week" tickLine={false} axisLine={false}/><YAxis tickLine={false} axisLine={false}/><Tooltip contentStyle={{borderRadius:12,border:"1px solid #D8C9B5"}}/><Area type="monotone" dataKey="completion" stroke="#D85032" strokeWidth={3} fill="url(#completion)"/></AreaChart></ResponsiveContainer></div><div className="row" style={{flexWrap:"wrap"}}><span className="tag">10 completed</span><span className="tag">6 in progress</span><span className="tag">2 not started</span></div></section><section className="panel"><p className="eyebrow muted">Live updates</p><h2 className="panel-title">Recent activity</h2><div className="activity-list">{managerActivity.map(item=><div className="activity" key={item.id}><span className="icon-box"><Clock3 size={17}/></span><div><strong>{item.person}</strong><div className="muted" style={{fontSize:13}}>{item.action}</div><small className="muted">{item.time}</small></div></div>)}</div></section></div><div className="manager-grid" style={{marginTop:20}}><section className="panel attention"><div className="row between"><div><p className="eyebrow" style={{color:"var(--chilli)"}}>Needs attention</p><h2 className="panel-title">Support before the next shift</h2></div><Link href="/manager/employees" className="btn btn-dark">View employees <ArrowRight size={17}/></Link></div><div className="module-list">{employees.filter(e=>e.status==="Needs attention").map(e=><div className="module-row" key={e.id}><span className="icon-box">{e.initials}</span><div><strong>{e.name}</strong><div className="muted">{e.role} · {e.station}</div></div><div><strong>{e.progress}%</strong><div className="progress"><span style={{width:`${e.progress}%`}}/></div></div><span className="tag">{e.status}</span></div>)}</div></section><section className="panel"><p className="eyebrow muted">Starting soon</p><h2 className="panel-title">Upcoming new hires</h2><div className="activity-list"><div className="activity"><span className="icon-box">MC</span><div><strong>Maya Chen</strong><div className="muted">Server · Starts August 3</div></div></div><div className="activity"><span className="icon-box">OK</span><div><strong>Owen King</strong><div className="muted">Dishwasher · Starts August 6</div></div></div></div></section></div></>}
function ManagerStat({icon,value,label}:{icon:React.ReactNode;value:string;label:string}){return <div className="stat-card"><span className="icon-box">{icon}</span><span className="stat-number">{value}</span><span className="muted">{label}</span></div>}

import Link from "next/link";
import { ArrowRight, Lock } from "lucide-react";
import { trainingModules } from "../../data/mock-data";

export function TrainingList(){return <><div className="section-head"><div><p className="eyebrow" style={{color:"var(--chilli)"}}>Entrenamiento</p><h1 className="display">My training</h1><p className="muted">Complete the essentials before practical station coaching.</p></div><span className="tag">7 assigned modules</span></div><div className="training-grid">{trainingModules.map(module=><article className="card training-card" key={module.id}><img src={module.image} alt={`${module.title} training placeholder`}/><div className="card-body"><div className="row between"><span className="tag">{module.microLabel}</span><strong>{module.progress}%</strong></div><h2>{module.title}</h2><p className="muted">{module.lessons} lessons · {module.duration} minutes</p><div className="progress"><span style={{width:`${module.progress}%`}}/></div><div className="row between" style={{marginTop:20}}><span className="muted" style={{textTransform:"capitalize"}}>{module.status.replace("-"," ")}</span>{module.status==="locked"?<span className="tag"><Lock size={14}/> Complete Fryer first</span>:<Link className="btn btn-outline" href={`/employee/training/${module.id}`}>{module.status==="completed"?"Review":"Open"}<ArrowRight size={16}/></Link>}</div></div></article>)}</div></>}
